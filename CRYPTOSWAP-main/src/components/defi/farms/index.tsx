import PrestamosDeFiPrototype from '@/components/defi/farms/prestamos';

export default function PrestamosPage() {
  return <PrestamosDeFiPrototype />;
}

// Exportación duplicada (comentada pero presente)
// export default function FarmsPage() {
//   return <Farming />;
// }

// Eliminar importaciones y componentes no utilizados
